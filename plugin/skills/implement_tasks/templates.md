# Moved

This skill was renamed to `implement_inline`. This pointer file exists so sessions holding a stale pre-rename skill body still resolve their reads instead of erroring.

Read [../implement_inline/templates.md](../implement_inline/templates.md) and use it as directed.
